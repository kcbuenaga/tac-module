# TAC Module Configuration
# Generated by BMAD Module Builder
# Date: 2026-01-24

# Core Configuration Values (from BMAD Core)
user_name: {user_name}
communication_language: {communication_language}
document_output_language: {document_output_language}
output_folder: {output_folder}

# TAC Module Specific Configuration
thesis_artifacts: "{project-root}/_bmad-output/thesis-artifacts"
institution_name: "USP"

# Language Preferences
# This overrides communication_language for TAC workflows
# Can be: 'english', 'portuguese', 'auto' (detect from conversation)
# If 'auto', agent will detect language from user's first message and update this field
preferred_tac_language: "auto"
